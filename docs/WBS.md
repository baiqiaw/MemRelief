# WBS：MemRelief（工作分解结构）

## 文档信息
- 版本：v1.0
- 日期：2026-09-05
- 上游：[PRD v1.3](PRD.md)、[system-spec](specs/system-spec.md)、[modules/\*](specs/modules/)（五模块规格）、[data-contracts](specs/interfaces/data-contracts.md)（共享契约）
- 模式：从 PRD 生成（新建）
- 标记说明：`[T-NN]` 任务ID（稳定身份，永不重排/复用）、`[WP]` 工作包（可派单）、`[Xh (高/中/低)]` 估算工时+置信度、`[汇总: Xh (置信度)]` 父节点=子工作包之和（置信度取子节点最低）、父节点 `[来源]`=子工作包来源并集

---

## WBS 树

- 0. MemRelief [汇总: 408h (中)]
  - 1. 扫描判定引擎（Core: scanner + rules）[来源: R01, R02, R03, R05] [汇总: 144h (中)]
    - 1.1 [T-01] 快照与基础采集器 [WP] [16h (高)]
    - 1.2 [T-02] 进程活动信号采集器群 [WP] [24h (中)]
    - 1.3 [T-03] 来源信号采集器群 [WP] [24h (中)]
    - 1.4 [T-04] 候选验签与缓存 [WP] [16h (中)]
    - 1.5 [T-05] 内存概览采样 [WP] [8h (高)]
    - 1.6 [T-06] 三级判定与冲突消解引擎 [WP] [40h (中)]
    - 1.7 [T-07] 判定辅助（预筛/查询/预标） [WP] [16h (高)]
  - 2. 释放引擎（Core: releaser）[来源: R03, R04] [汇总: 56h (中)]
    - 2.1 [T-08] 释放规划与树保护 [WP] [16h (高)]
    - 2.2 [T-09] 两段式执行主链 [WP] [24h (中)]
    - 2.3 [T-10] 取消与结果报告 [WP] [16h (中)]
  - 3. 数据持久化（Core: storage）[来源: R01, R04, R06] [汇总: 32h (高)]
    - 3.1 [T-11] 白名单存储 [WP] [12h (高)]
    - 3.2 [T-12] 释放日志存储 [WP] [12h (高)]
    - 3.3 [T-13] 名单资源装载 [WP] [8h (高)]
  - 4. 桌面应用（App: ui + 组合根）[来源: R01–R06] [汇总: 96h (中)]
    - 4.1 [T-14] 应用骨架与组合根 [WP] [24h (中)]
    - 4.2 [T-15] 三级推荐列表 [WP] [32h (中)]
    - 4.3 [T-16] 释放交互闭环 [WP] [24h (中)]
    - 4.4 [T-17] 内存概览条 [WP] [8h (高)]
    - 4.5 [T-26] 白名单管理面板 [WP] [8h (高)]
  - 5. 工程基础设施 [来源: 基础交付物] [汇总: 16h (高)]
    - 5.1 [T-18] 解决方案骨架与质量门禁 [WP] [8h (高)]
    - 5.2 [T-19] 单文件发布配置与自用说明 [WP] [8h (高)]
  - 6. 测试与验收支撑 [来源: R01–R06（PRD §3.2 前置构造规格）] [汇总: 40h (中)]
    - 6.1 [T-20] 孤儿测试进程构造器 [WP] [16h (中)]
    - 6.2 [T-21] 真机扫描验证台 [WP] [8h (高)]
    - 6.3 [T-22] GWT 验收矩阵与真机验收 [WP] [16h (中)]
  - 7. 项目管理 [来源: 基础交付物] [汇总: 24h (高)]
    - 7.1 [T-23] 工作计划 [WP] [8h (高)]
    - 7.2 [T-24] 阶段验收记录 [WP] [8h (高)]
    - 7.3 [T-25] 经验总结 [WP] [8h (高)]

---

## WBS 词典

| 任务ID | 编码 | 工作包 | 描述 | 验收标准 | 估算 | 依赖 | 来源 |
|--------|------|--------|------|----------|------|------|------|
| T-01 | 1.1 | 快照与基础采集器 | scanner 基座：进程快照枚举（PID/PPID/名称/路径/创建时间/私有提交/命令行[WMI 通道]/所有者）与 SignalFailure 失败记录框架；口径 #1 数据侧、#13 | 真机 300–600 进程采集段 ≤2.0s；字段与 data-contracts `ProcessSnapshot` 一致；采集失败逐项落 `Failures` 不静默置空（法-3 数据基础）；单测覆盖字段映射与失败路径 | 16h (高) | T-18 | R01 |
| T-02 | 1.2 | 进程活动信号采集器群 | 口径 #4（EnumWindows+DWM cloaked+UWP 特例）、#6（GetExtendedTcpTable 仅 ESTABLISHED）、#7（CPU 差分）、#8（服务关联+QueryServiceConfig2 失败恢复）、#10（系统目录清单，WindowsApps 不触发🚫链路） | 五信号按口径表实现且各有失败兜底落 `SignalFailure`（对应进程不进✅级）；单元级以构造数据对测判定输入 | 24h (中) | T-01 | R01 |
| T-03 | 1.3 | 来源信号采集器群 | 口径 #3（同目录存活+路径归一化）、#12（Run 键 64+32 双视图+StartupApproved+启动文件夹用户/公共+ITaskService 手写 COM 仅根与用户自定义文件夹）、#15（计划任务拉起） | 四类来源含条目名可采集（路径精确匹配关联，不按进程名）；单类失败→来源"采集失败"且不进✅级；ITaskService 不采集 \Microsoft\Windows 内建任务 | 24h (中) | T-01 | R01, R02 |
| T-04 | 1.4 | 候选验签与缓存 | 口径 #9：WinVerifyTrust 仅对候选执行（系统目录核心命中者不验签按微软处理）；结果按"路径+mtime"缓存 | 四分支映射 `SignatureStatus` 六值；缓存生效后重复扫描验签 ≤0.5s；Access Denied→Unverifiable 按受保护处理 | 16h (中) | T-01 | R01 |
| T-05 | 1.5 | 内存概览采样 | R05 数据源：NtQuerySystemInformation（手写例外通道）优先，PDH 三计数器兜底；standby 不可得降级 | 与资源监视器同分钟对照误差 ≤10%（R05 GWT）；降级时 Standby=null 且 Source=Degraded | 8h (高) | T-18 | R05, R03 |
| T-06 | 1.6 | 三级判定与冲突消解引擎 | rules 核心：15 口径求值+四名单白名单匹配派生+冲突消解（🚫>⚠️>✅）+孤儿/模式库豁免+树合计 `TreePrivateBytes`+保守兜底；纯函数（合成数据先行，不阻塞采集器开发） | 同输入同输出、零 I/O 零状态；PRD §3.2 R01 全部 GWT 以合成数据单测通过（孤儿/PID 复用/旁证降级/模式库豁免/冲突消解/UWP/常驻/PPL/元数据不可读/空态）；Failures 或名单缺失→相关进程不进✅级 | 40h (中) | T-18 | R01, R02 |
| T-07 | 1.7 | 判定辅助 | `CandidateIds` 候选预筛（潜在✅/⚠️且非系统目录）、`Query` 全量判定查询（含"未命中规则"/"白名单排除"）、`RequiresElevation` 预标（服务口径#8+跨用户/受拒，依据入 Bases） | 预筛结果驱动 CollectSignatures（契约时序）；Query 覆盖 R02 搜索框 GWT；预标字段贯通 ui 高亮与释放分类 | 16h (高) | T-06 | R01, R02, R03 |
| T-08 | 2.1 | 释放规划与树保护 | `Plan`：以勾选项为根构建进程树、树保护集（🚫保护名单∪白名单）标记跳过节点及子树、树合计内存；树构建唯一承载（Execute 复用） | `TreePlan` 含 SkippedNodes（节点+原因）与 TreePrivateBytes（确认弹窗"N 树/X MB"数据源）；R03 树保护 GWT 通过 | 16h (高) | T-01, T-13 | R03, R04 |
| T-09 | 2.2 | 两段式执行主链 | 身份校验（进程名+创建时间对快照）→WM_CLOSE 优雅→3s→TerminateProcess；无窗口项跳过优雅直杀；多树并行+`TreeProgress` 逐树进度 | 身份不一致→IdentityChanged 跳过不执行；单树 ≤5s（含 3s 等待）；无窗口直杀路径生效；R03 树释放 GWT 通过 | 24h (中) | T-08 | R03 |
| T-10 | 2.3 | 取消与结果报告 | `Cancel`（跳过未开始+等待进行中，不中断强杀）；8 值 Outcome 分类（Access Denied 按所有者/令牌二分 NeedsElevation/Blocked+错误码）；双释放量+释放前后 MemoryOverview 采样 | 取消语义符合法条（防半完成态）；主释放量=快照私有提交合计、校验释放量可负如实输出；`ReleaseReport` 字段与契约 §1.3 一致 | 16h (中) | T-09, T-05 | R03 |
| T-11 | 3.1 | 白名单存储 | `IWhitelistStore`：Add/Remove/List/Snapshot（不可变一致视图）+元数据；用户数据目录锚定（提权重启后同目录）；损坏自愈+原子写 | 损坏→改名 .corrupt 保留+重建空白（R04 GWT）；文件 schema 走契约变更流程；仅白名单/日志两自有文件（法-4） | 12h (高) | T-18 | R04 |
| T-12 | 3.2 | 释放日志存储 | `IReleaseLogStore`：JSONL 追加（PRD F6 schema，时区/字段映射归落盘层）+5MB×3 轮转+行级自愈；写失败不阻塞释放 | 5MB 滚动保留最近 3 副本（R06 GWT）；损坏行跳过可解析；写失败返回结果供 ui 呈现"未留痕" | 12h (高) | T-18 | R06 |
| T-13 | 3.3 | 名单资源装载 | `IRulePackStore`：四份内置名单（残留模式库/常驻应用/安全软件/系统保护）加载为 `RulePack`；保护类失败上报（fail-safe）；含首版基线文件入库（常驻/安全软件名单由用户补齐本机实际值，就绪时点 M2 开发前） | 加载失败→编排方使 rules 保守兜底，禁空名单放行；四份基线与 PRD 附录清单一致 | 8h (高) | T-18 | R01, R04 |
| T-14 | 4.1 | 应用骨架与组合根 | WPF 主窗口布局骨架（概览条/搜索框/列表区/操作栏；列表与释放控件由 T-15/T-16 落地）；五态状态机宿主（显式枚举+单一转换入口+控件可用性矩阵）；组合根注入扫描/判定链（storage 接线随 T-15/T-16/T-26 落地）；扫描链四步编排+`ScanFailed` 处理 | 状态机五态转换与 PRD §3.6 一致（进行中态禁用矩阵生效）；扫描失败保留旧结果不展示半成品；扫描异步执行、UI 不冻结（PRD §3.4）；Core 编译产物无 UI 程序集引用（法-1 检查通过） | 24h (中) | T-18, T-01, T-02, T-03, T-04, T-06, T-07, T-13 | R01, R02, R03 |
| T-15 | 4.2 | 三级推荐列表 | ✅默认勾选/⚠️默认不勾/🚫折叠仅计数不可勾；组内按树合计降序；行展开六要素；搜索框全量查询；右键加白（仅✅/⚠️）；依据→中文文案映射与空值口径 | R02 全部 GWT 通过（六要素齐全、孤儿"(父进程已退出)"、无来源"无（手动启动）"、搜索含不可见原因、🚫展开可见原因、服务拉起文案） | 32h (中) | T-14, T-11 | R02, R04 |
| T-16 | 4.3 | 释放交互闭环 | 确认弹窗（N 树/X MB）→Execute+进度+取消→结果报告（双释放量/跳过说明/被拉起提示/日志写结果）→已结束项移除+"可重新扫描"；管理员重启编排（入口+启动参数+自动重扫+失败项高亮不自动勾选）；日志追加编排单路径 | R03 交互类 GWT 全部通过（确认取消停留已展示、取消语义、管理员重启高亮重勾、移除已结束项）；释放中关窗=取消等待收尾 | 24h (中) | T-15, T-08, T-10, T-12 | R03, R06 |
| T-17 | 4.4 | 内存概览条 | 概览三数值+口径说明文案；启动/扫描后/释放后三时点刷新 | R05 GWT 通过；读失败显示"—"提示不阻塞主流程 | 8h (高) | T-14, T-05 | R05 |
| T-18 | 5.1 | 解决方案骨架与质量门禁 | sln 三项目（Core/App/Tests，编译期隔离）；门禁脚本检查 Core 编译产物程序集引用清单（防 NuGet 传递引入 UI）；xUnit+coverlet 接入 | 三项目编译通过；门禁脚本可独立运行并正确判定；Core 覆盖率 >80% 阈值生效 | 8h (高) | — | 基础交付物 |
| T-19 | 5.2 | 单文件发布配置与自用说明 | self-contained 单文件发布配置+发布产物验证；自用 README（使用要点/数据目录位置/已知边界） | 发布 exe 免安装可直接运行；README 覆盖数据目录与便携换机取舍 | 8h (高) | T-14 | 基础交付物 |
| T-20 | 6.1 | 孤儿测试进程构造器 | 测试程序对：父启动子后退出、子申请 >50MB、无窗口；参数化构造降级场景（ESTABLISHED 连接/同目录存活进程/常驻名命中/CPU 忙/UWP 旁路用真机实存项） | 满足 PRD §3.2 前置构造规格全部条件（避开所有降级信号）；场景参数可脚本启停；被 R01/R03 真机验收复用 | 16h (中) | T-18 | R01, R03 |
| T-21 | 6.2 | 真机扫描验证台 | 命令行驱动四步扫描链（TakeSnapshot→CandidateIds→CollectSignatures→Classify），输出分级明细+判定依据+分段计时；M1 验收载体（PRD M1"命令行/测试先行验证"） | 真机 300–600 进程引擎段合计 ≤2.8s（system-spec §7 分解：采集 2.0+验签 0.5+判定 0.3，分段计时可见）；端到端 3s（含渲染 0.2s）归 T-22 验收；输出可对照基线采样数据（scripts/baseline-result.txt）人工复核推荐准确性 | 8h (高) | T-01, T-02, T-03, T-04, T-06, T-07, T-13 | R01 |
| T-22 | 6.3 | GWT 验收矩阵与真机验收 | PRD §3.2 全部 GWT 与 §3.4 量化非功能映射"引擎级单测/真机手工"两类并执行；M1/M2/M3 三阶段真机验收执行与记录 | 每条 GWT 有执行记录（测试名或手工步骤+结果）；端到端 ≤3s（含渲染）、整批释放 ≤30s、自身工作集 ≤100MB（600 进程扫描后峰值）真机实测通过（PRD §3.4 测量条件）；缺陷阻塞项清零后方可阶段通过 | 16h (中) | T-16, T-17, T-19, T-20, T-26 | R01–R06 |
| T-23 | 7.1 | 工作计划 | 采纳 schedule-planner 排期结果；建立里程碑跟踪表（M1/M2/M3） | 排期窗口回写工作包跟踪表；关键路径与 P80 工期记录在案 | 8h (高) | — | 基础交付物 |
| T-24 | 7.2 | 阶段验收记录 | M1/M2/M3 各一次阶段验收（结论+证据链接：测试输出/验证台输出/验收矩阵）；M1/M2 阶段验收记录随阶段产生落档，本包于 M3 收尾汇总归档 | 三阶段各有验收记录且证据可点开复核 | 8h (高) | T-21, T-22 | 基础交付物 |
| T-25 | 7.3 | 经验总结 | 项目复盘+v2 商用遗留移交清单（.NET 升级评估/签名/遥测尽调/兼容矩阵/白名单粒度等） | 遗留清单逐项入 issue 台账，无仅存对话文本的遗留项 | 8h (高) | T-24 | 基础交付物 |
| T-26 | 4.5 | 白名单管理面板 | 白名单管理面板（元数据+逐项移除）；列表尾部白名单计数折叠区；"打开日志/数据目录"入口 | 移除白名单条目后重扫恢复参与判定（R04 GWT）；面板条目含添加时间/路径/备注元数据 | 8h (高) | T-14, T-11, T-12 | R04, R06 |

---

## 附：里程碑映射（信息性，排期归 schedule-planner）

| 里程碑（PRD §2.2） | 工作包 |
|---|---|
| M1 扫描引擎核心 | T-18, T-01, T-02, T-03, T-04, T-06, T-07, T-13, T-20, T-21 |
| M2 GUI | T-05, T-08, T-09, T-10, T-11, T-12, T-14, T-15, T-16 |
| M3 收尾 | T-17, T-26, T-19, T-22, T-23, T-24, T-25 |

> 交叉项说明：T-05 概览采样名义 M3（R05 P1）但 T-10 校验释放量依赖其采样，实际随 M2 落地；T-11 白名单存储提前至 M2（T-15 右键加白与 T-26 折叠计数依赖）；T-12 日志存储随 M2 落地（T-16 日志追加编排依赖；R06 名义 P1）。以排期结果为准。

## 修订记录

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-09-05 | 初始版本：7 交付物 × 26 工作包，408h（中置信） |
