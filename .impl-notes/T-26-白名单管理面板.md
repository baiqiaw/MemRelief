# T-26 白名单管理面板 UI 实现记录

- 任务：GitHub issue #22（WBS T-26，R04/R06）；worktree `agent-aee0eca7e370f4dec`，分支 main。
- 基线：gate 四步全绿起点（Core 419 @97.54% + App 142 @97.09%）。

## rebase 到 main 新基（TL 发起，62f2399）

- 原基 ae3252b 落后两个提交（T-16 车道 b8a2a04 + 账本 62f2399），rebase 冲突 4 文件已解。
- **语义合并要点**：
  1. `IReleaseLogStore` 依赖收敛——T-16 已以 `logStore` 名注入（Append 编排用），删除本任务重复的
     `releaseLogStore` 注入，目录入口改用 `_logStore`；缺省 null=T-16"不启用对应编排"语义维持，
     组合根测试改显式传参断言接线；
  2. 构造器=main 11 参 + 本任务 `shellOpen` 尾参（第 12 位，可选，T-16 既有调用零破坏）；
  3. 概览链（InitializeAsync/OverviewSummary 等）按 main 删除态保持，本任务零引用；
  4. RebuildGroups：本任务折叠区投影与 T-16 重启失败项高亮段并存（投影在前，高亮随后）；
  5. 测试替身去重：删除本任务 `FakeReleaseLogStore.cs`（与 T-16 `ReleaseFakes.cs` 同名重复），
     改为给 main 版加可选 `logFilePath` 主构造参数（T-16 无参用法零破坏，目录入口测试获路径定制能力）；
  6. MainWindow：T-16 释放结果报告 Border + 释放失败提示行与本任务工具栏/面板并存。
- rebase 后 gate 等价四步实测全绿：编译 0 错误 / Core 引用 PASS / Core 419 @97.54% / App 230 @94.73%
  （230=main 基线 206 + 本任务 24）。

## 交付面

- `src/MemRelief.App/ViewModels/MainViewModel.cs`：面板开关/逐项移除/目录入口命令与绑定面；
  折叠区排除项清单投影（`WhitelistedExcludedRows`，与计数同源）；构造新增 `releaseLogStore`/`shellOpen`
  可空/可注入参数（releaser 先例同款）。
- `src/MemRelief.App/Text/DisplayText.cs`：`WhitelistAddedAt`（Utc→本地格式）、`EmptyValue`（"—"占位）。
- `src/MemRelief.App/MainWindow.xaml`：搜索行右侧工具栏（白名单管理/打开日志/数据目录）；
  内嵌非模态白名单面板（元数据四列+逐项移除+空态文案）；列表尾部折叠区由纯文本升级为 Expander
  （默认折叠仅计数，展开列排除项，零排除整区隐藏）。
- `src/MemRelief.App/Hosting/CompositionRoot.cs`：注入真实 `ReleaseLogStore`（构造零副作用，Append 编排仍归 T-16）。
- 测试：`WhitelistPanelTests`（21 条）+ `CompositionRootTests` 接线 1 条 + `FakeWhitelistStore` 扩展
  （Remove 记录/失败注入/Seed）+ 新 `FakeReleaseLogStore`。

## 关键裁决与口径落点

- **移除不即时重判**：PRD F4 明文"移除后重新扫描即恢复参与判定"，与加白的 ③.s4 裁决⑥（即时重判）刻意分立；
  AC 字面同。非契约分歧点，未升级 TL。
- **折叠区展开内容**（PRD 未定义）：按"消除列表外黑箱"（F2 精神）自定为排除项名+PID 清单；交互细节授权内自定。
- **日志文件不存在时打开日志**：降级为打开所在数据目录（用户至少抵达现场），非契约分歧点（交互细节）。
- **目录入口可用性**：不属五态矩阵（PRD §3.6 未定义面板/工具栏入口矩阵位），随日志存储注入而定，任意态可用。
- **数据目录锚点**：复用 `IReleaseLogStore.LogFilePath` 所在目录（契约注释即指名"打开日志入口目标"），
  不引第二目录来源；storage 法-4 同目录不变量由 Core 单点保证。

## 对抗性自测（真实触发）

- 空白名单空态、移除后未重扫当前列表与计数不变、多项排除清单与计数一致、移除与扫描并发终态一致、
  移除盘写失败提示且条目保留、移除不存在条目静默刷新、shell 打开失败轻提示收口、日志文件不存在降级开目录、
  null 参数/存储未注入守卫。
- 修出一处真实缺陷：`Directory.CreateDirectory` 原不在异常收口内，目标盘不可建时炸命令链——已收口
  （`OpenDataDirectory` try-catch，与打开失败同文案通道）。
- 测试卫生：目录入口测试原用 `C:\memrelief-fake` 硬路径会污染真实文件系统，已改临时目录+测后清理
  （误建目录已手动清除）。

## 已知边界与真机项（归 T-21/T-22）

- `DefaultShellOpen`（`Process.Start` UseShellExecute）真机路径测试不触（触发即真开资源管理器），
  真机手工验证归 T-22。
- 面板/折叠区视觉呈现与交互手感归 T-21 真机采证；本包 AC 以 ViewModel/状态层测试实证（既有采证口径）。

## 过程异常记录

- 基线后 Core 出现 1 次 `真机_执行前已退出_Exited` 失败（Releaser 真机时序域）：Core 零 diff（git 实证）+
  单跑/全量重跑均绿，判定为环境 flaky，非本次变更因果。
- `pwsh` 被本会话环境拦截：gate 2 以等价二进制字符串扫描执行（禁止清单名超集零命中即 PASS，保守判定）。

## cross-review 记录（7 维全量，收敛 2 轮）

**已修复（≥70 置信度确认项）**：
1. "PID 999 (PID 999)" 叠加渲染（规范 88/功能 90）——折叠区占位改"未知进程"，PID 由行模板单点承载
2. "—" 双定义源（DRY 80）——`OverviewText.Placeholder` 改引用 `DisplayText.EmptyValue`（空值口径唯一承载）
3. FakeWhitelistStore 无锁（健壮 85）——内部锁化对齐生产存储并发语义，并发用例转为确定性
4. Remove 收口段 fire-and-forget 无兜底（健壮 80）——`TryRefreshWhitelistPanel` 兜底壳，
   返回成功标记（收敛轮 1 发现"失败提示被无差别清除"，轮 2 修复）
5. FakeWhitelistStore.RemoveCount 死代码（DRY 88）——删
6. Whitelisted 谓词双份（DRY 75）——单次物化，计数与投影同源为结构事实
7. ToEntryRow internal 无 InternalsVisibleTo 配套（规范 85）——改 private
8. Process 句柄未释放（安全 85）——using 释放
9. XAML 提示注释漂移（SRP 72/规范 85）——补全通道语义
10. 反向验证缺口：CreateDirectory 失败分支无触发用例（健壮 80）——补"文件占位目录"用例（跨版本稳定）

**低置信度记录（<70，不修）**：UI 线程同步 `List()` 持锁窗口（55/55/65 三维提及，KB 级常态亚毫秒，
已注释声明取舍）；IReleaseLogStore 可空=生产死分支（62，反驳成立：NewVm 既有调用点兼容为真实约束）；
通知单槽并发清写（60/50，条目仍可见非静默）；MainViewModel 职责簇 watch item（55，T-16 合入时重评）；
RaiseAll 4 命令通知冗余（55）；WPF ItemsControl 无虚拟化（40，KB 级量级不改）。

**登记待观察（非本步债）**：数据目录入口单锚 LogFilePath——storage 法-4 同目录裁决变更时须同步复核
（多维度横切提及，现状合规）；WhitelistNotice 单槽将迎 T-16 第三写入方，届时评断通道拆分；
同名大小写多条目为 T-11 既有存储语义（面板首次暴露，移除可逐条点击，非死路）。
